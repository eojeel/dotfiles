- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.aliases: shell/.aliases
    ~/.gitconfig: .gitconfig
    ~/.zshrc: .zshrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
